# CONTINUATION_PROMPT.md

Paste everything inside the box below as the FIRST message in another Claude account, and attach the latest project zip.
Claude refreshes this file (and the copy at the end of every reply) each round. Last refreshed: 19 September 2026.

```
CONTINUATION PROMPT (Cheliv / Compassionate Care Plus platform)

I'm attaching the latest project zip. Read EVERY code file and every .md file top to end (start with docs/PROJECT_HANDOFF.md, NEXT_STEP.md, PHASE_STATUS.md, DOCUMENTS.md, CARE_PLANS.md, VISITS.md). Clone https://github.com/Justixxprime/compassionate-care-plus.git and compare it to the zip. Find the exact stopping point before building. Explain non-coding things in ultra-simple language. End EVERY reply with a fresh, updated copy of this prompt, because I move between Claude accounts and come back to earlier ones.

STANDING RULES: no em dashes anywhere; no "placeholder / to be confirmed" wording on the public site; the four footer legal links stay unlinked and unflagged; cinematic, premium visual bar for public pages; every delivery includes a full project zip (exclude .env, node_modules, .next) plus present the files; full file contents, never diffs; after zipping, extract it fresh and diff against the working files; name-check src/app/layout.tsx (must have html, body, globals.css import) and src/app/(public)/page.tsx every time and say so; terminal and git commands at the END of every delivery. Next.js 16.3.5: read node_modules/next/dist/docs before writing code. Prisma pinned 6.19.3, no new dependencies. The phone number is settled, do not raise it. Never ship migration SQL: I run `npx prisma migrate dev --name <name>` myself. Never put my .env in a zip. Zips never delete files, so name any file I must Remove-Item myself.

STATE (19 Sep 2026): Milestones A, B, C done. Milestone D: patients, care team, visits and care plans done and confirmed on my machine (my verify:access run passed 175 checks; the red "1 Issue" badge is gone). DOCUMENTS written and verified in the build environment (tsc, eslint, next build clean; verify:access 254 passed, 0 failed; eight rules deliberately broken in a copy and the script failed on those each time; real HTTP against the built app as admin, nurse, nurse2 checked downloads and page renders). NOT yet run on my machine, and the upload form's server action was not tested over real HTTP (no browser in the build environment). Files: Document and DocumentFile models; src/lib/documents.ts, document-constants.ts, documents-actions.ts; src/app/documents/* including [id]/download/route.ts; prisma/demo-pdf.ts; seed adds 5 synthetic documents (Eleanor: consent, physician order, insurance card; Marcus: consent, photo ID; insurance and identification are restricted); verify-access.ts sections 2c and 11a to 11f; next.config.ts raises the Server Action body limit to 3 MB; docs/DOCUMENTS.md.

FIRST, ask me: did `npx prisma migrate dev --name add_documents`, `npx prisma db seed` (expect "5 synthetic demo documents ready"), `npm run verify:access` (expect 254 passed), the /documents click-through as all three demo accounts, and a real upload of a small PDF through the form (plus a renamed .txt, an over-2MB file and a repeat, which should each be refused) work? Also try opening the admin's insurance-card download link while signed in as the nurse (expect "That document could not be found."). Fix anything I report before building more. If I say everything worked, do not ask me to screenshot each step: continue building.

RULES (do not weaken). VISITS: permission (visits.read/create/update) AND relationship to the patient. Administrative roles (SUPER_ADMIN, ADMIN, CLINICAL_SUPERVISOR, CARE_COORDINATOR) see all and can schedule for any active care-team member of an active patient. Others see visits of actively assigned patients, schedule only for themselves, change only their own. scheduled -> in_progress -> completed; cancel and mark_missed only from scheduled; final states final. No double-booking. Times stored UTC, shown America/Chicago. CARE PLANS: THREE questions: permission (care_plans.read/create/update/approve), relationship (getPatientScope), and for WRITING also being on that patient's care team right now (isActiveCareTeamMember), so SUPER_ADMIN reads and approves but cannot write for a patient it is not on the team of. Any team member can edit a draft. The author can never approve their own plan (four eyes). Approving needs at least one goal and no other active plan for the patient. draft -> active -> completed, draft -> archived; final states final. Active wording and goals are locked, only marking a goal met works. One draft and one active plan per patient. Denials audited as access_denied with vague not-found messages for anything out of reach. DOCUMENTS: THREE questions: permission (documents.read/upload/delete), relationship (getPatientScope), and CATEGORY: insurance and identification are restricted to administrative roles, and to a nurse a restricted document does not exist (missing from lists, and the download route returns the same 404 words as a made-up id). Unknown categories fail closed. No care-team question for documents. File type read from the bytes, PDF/PNG/JPEG only, 2 MB, no duplicate bytes per patient, active patients only. Archive needs documents.delete (SUPER_ADMIN only), hides everywhere, keeps the row and bytes. Downloads audited (document_downloaded). Bytes live in the database for the demo only: real patient files need encrypted object storage and virus scanning, a paid decision, ask me first.

NEXT SLICE: referrals (the last slice of Milestone D), then a full review pass and a proposal for Milestone E (the real staff-facing screens), which you ask me about before starting. Design referrals like patients, visits, care plans and documents: real schema addition, a real access-control question (who can see, create and update a referral, and what is the sensitive part), seed data that makes it testable with the existing demo accounts, a new section in scripts/verify-access.ts that tries to break it (and prove the test can fail by breaking a rule in a copy), reuse getPatientScope and isActiveCareTeamMember instead of copying the rules. No paid services without asking me first. Update docs (CHANGELOG, PHASE_STATUS, NEXT_STEP, PROJECT_HANDOFF, a new topic doc, docs/CONTINUATION_PROMPT.md).

OPEN DECISIONS FOR ME: nurses see colleagues' visits on shared patients but cannot change them; any nurse on the team can edit a draft plan, not only its author; only ADMIN and SUPER_ADMIN can approve plans while CARE_COORDINATOR and CLINICAL_SUPERVISOR hold no permissions yet; where real document files will live (encrypted object storage, priced by you with me first); whether ADMIN may archive documents and whether consents should be restricted; the repo is public and PROJECT_HANDOFF.md names the owner and says the site is a surprise (private repo or trim the file?).

SANDBOX RECIPE for real testing (build environment cannot reach binaries.prisma.sh): apt-get update then apt-get install -y postgresql, start with pg_ctlcluster 16 main start inside every command (it stops between calls, so does any background server), set the postgres password and create a database, DATABASE_URL must contain @localhost; work in a COPY of the project: npm ci --ignore-scripts, add engineType = "client" to the generator, npm i --no-save --ignore-scripts @prisma/adapter-pg@6.19.3 pg@8, pass the PrismaPg adapter in src/lib/prisma.ts and prisma/seed.ts, set PRISMA_SCHEMA_ENGINE_BINARY and PRISMA_QUERY_ENGINE_LIBRARY to a dummy executable plus PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1, prisma generate, apply migration SQL folders with psql (hand-write SQL for new models, sandbox only), npx next typegen before tsc, next build works (run npx next build directly, not npm run build), scripts run with tsx --tsconfig tsconfig.scripts.json, render pages by inserting a sessions row and curl with cookie ccp_session=<id> against next start (start it in the same command as the curl calls). Never change the shipped source for this.

MY COMMANDS after each delivery (Windows PowerShell): cd C:\Users\LENOVO\OneDrive\Desktop\compassionate-care-plus ; npm install ; npx prisma migrate dev --name <name> ; npx prisma db seed ; npm run verify:access ; npm run dev ; then git add . ; git commit ; git push.
```
