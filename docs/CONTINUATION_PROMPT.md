CONTINUATION PROMPT (Cheliv / Compassionate Care Plus platform)

I'm attaching the latest project zip (ccp-family-portal.zip). Read EVERY code file and every .md file top to end (start with docs/PROJECT_HANDOFF.md, NEXT_STEP.md, PHASE_STATUS.md, APP_SHELL.md, CAREGIVER_PORTAL.md, PATIENT_PORTAL.md, FAMILY_PORTAL.md, DOCUMENTS.md, VISIT_NOTES.md, TASKS.md, NOTIFICATIONS.md, REVIEW_MILESTONE_D.md). Clone https://github.com/Justixxprime/compassionate-care-plus.git and compare it to the zip. Find the exact stopping point before building. Explain non-coding things in ultra-simple language. End EVERY reply with a fresh, updated copy of this prompt, because I move between Claude accounts and come back to earlier ones. PACKAGE AND PRESENT THE ZIP BEFORE spending tool calls on extra verification. Budget your tool calls so the zip is presented before you run out.

STANDING RULES: no em dashes anywhere (code, docs, replies); no "placeholder / to be confirmed" wording on the public site; the four footer legal links stay unlinked and unflagged; cinematic, premium visual bar for public pages; every delivery includes a full project zip (exclude .env, node_modules, .next) plus present the files; full file contents, never diffs; after zipping, extract it fresh and diff against the working files; name-check src/app/layout.tsx (must have html, body, globals.css import), src/app/(app)/layout.tsx (must NOT have html or body) and src/app/(public)/page.tsx every time and say so; terminal and git commands at the END of every delivery. Next.js 16.3.5: read node_modules/next/dist/docs before writing code (an error screen gets retry(), not reset()). Prisma pinned 6.19.3, no new dependencies. The phone number is settled, do not raise it. Never ship migration SQL: I run npx prisma migrate dev --name <name> myself. Never put my .env in a zip. Zips never delete files, so name any file I must Remove-Item myself. My delivered zips carry only migration_lock.toml and the earliest 3 migration folders (expected, not a bug): every schema change after that was applied by me with npx prisma migrate dev, so when verifying in a sandbox pull the real migration history from the GitHub clone. In the sandbox, hand-write SQL only for models GitHub lacks (sandbox only, never shipped).

STATE (24 Sep 2026): Milestones A, B, C, D done. E0 to E4 round 2 (visit notes, note addenda, tasks, notifications), E5 round 1 (CAREGIVER PORTAL, /caregiver) and E5 round 2 (PATIENT PORTAL, /my-care) are CONFIRMED on my machine (pushed to GitHub, commit af6d273). E5 round 3, the FAMILY PORTAL WITH CONSENT GATING, is DELIVERED in ccp-family-portal.zip and tested in the build environment with real Postgres: verify:access 668, verify:shell 81, verify:notes 93, verify:tasks 40, verify:notifications 44, verify:caregiver 75, verify:portal 53, verify:family 156, tsc, eslint and next build clean, pages rendered 200 as family, admin, patient, nurse and caregiver, nineteen rules broken on purpose and each caught. I am now running the commands on my machine: npx prisma migrate dev --name add_family_consents (new table family_consents), then npx prisma db seed (adds permissions family.read and consents.manage, 38 permissions now; gives AUTHORIZED_FAMILY exactly family.read; gives ADMIN consents.manage; creates demo.family@cheliv.test, same demo password, Claire Whitfield, plus one consent from Eleanor Whitfield sharing her visit schedule and care team but NOT her care plan). Nothing to Remove-Item. If I paste output with FAIL lines, fix them first.
FAMILY PORTAL: screen /family ("Shared with me", menu item for holders of family.read, dashboard tile "Visits coming up" only for someone a patient has shared with). Office screen /consents ("Family access", Office menu group, holders of consents.manage: ADMIN and SUPER_ADMIN). Code: src/lib/family-portal.ts (reading), src/lib/family-consents.ts and family-consents-actions.ts (recording and withdrawing), src/lib/family-constants.ts (scopes visits, care_team, care_plan; relationships; durations 90 days, 1 year, until withdrawn; max 10 consents per patient), src/lib/patient-view.ts (the four loaders the patient portal and the family portal share: upcoming visits, recent visits, care team, ACTIVE plan; it decides no access), src/components/app/care-view.tsx (shared drawing), scripts/verify-family.ts (npm run verify:family). Model FamilyConsent (table family_consents): patient, family user, relationship, scopes String[], grantedBy, expiresAt (null = until withdrawn), revokedAt, revokedBy; written once, never edited, never deleted. Nothing can create a family account from a screen yet (seed only); that belongs with the staff account flow.

ASK ME ONLY THIS (once, after my normal commands succeed): (1) sign in as demo.family@cheliv.test: the menu shows only Dashboard and Shared with me; the page shows Eleanor Whitfield with visits and the care team and a plain line that she has not shared the care plan; (2) as Claire type /visits, /patients, /documents, /my-care and /consents: each shows the no-access screen; (3) sign in as demo.admin, open Family access, click Withdraw on Claire's permission, sign in as Claire again (she sees "Nobody has shared their care with you yet."), then as admin record it again (tick the confirmation box) and check Claire sees it again; (4) as demo.patient type /family: no-access screen. If all work, treat E5 round 3 as confirmed.

NEXT (once confirmed): my choice of (b) what an aide may WRITE (a short visit note and who reviews it) and READ about a patient; (c) patient messages or patient-visible and family-visible documents (each with its own permission and consent scope design); (d) open items: audit log pagination past 200 rows, a real "create staff account" flow (also creates family accounts and links a patient account) before Milestone F, /design-system publicly reachable, /request-care saves nothing, repo is public and PROJECT_HANDOFF.md names the owner and says the site is a surprise, whether supervisors should be kept away from restricted documents; (e) a small "who can see my care" panel on the patient's own My care screen. Full list in docs/REVIEW_MILESTONE_D.md.

RULES (do not weaken). VISITS: permission (visits.read/create/update) AND relationship. Administrative roles (SUPER_ADMIN, ADMIN, CLINICAL_SUPERVISOR, CARE_COORDINATOR) see all and schedule for any active care-team member of an active patient. Others see visits of actively assigned patients, schedule only for themselves, change only their own. scheduled -> in_progress -> completed; cancel and mark_missed only from scheduled; final states final. No double-booking. Times stored UTC, shown America/Chicago. CAREGIVER PORTAL: visits.checkin AND reach AND ownership, check in and out only; the caregiver role holds exactly visits.checkin + tasks.read; check in only on the visit's own office day; one place at a time (transaction locking the caregiver's user row); cancel and mark_missed refused ("ask the office"); missing, out of reach and a colleague's visit get the same words and an audited denial. PATIENT PORTAL: portal.read AND ownership (the one patient record linked to my account, my organization, active or on hold); read only; never notes, tasks, documents, referrals, drafts or other patients. FAMILY PORTAL: family.read AND consent AND scope. The consent must be for this account and this patient, not withdrawn, not run out, in my organization, patient active or on hold; the consent's scopes name which parts are shown (visits, care_team, care_plan) and an unnamed part is never queried; getFamilyCare takes no patient id; an account with no consent sees nobody; never notes, tasks, documents, referrals, messages, e-mails, date of birth, ids of people, drafts or other patients; the family role holds exactly family.read. CONSENTS: consents.manage AND reach; the form and the server require the office to confirm the patient's signed permission; the family account must be a real AUTHORIZED_FAMILY account of my organization and not the patient's own account; patient must be active or on hold; a made-up, discharged, unreachable or other-organization patient gets the same words; one consent in force per person and patient, at most 10 per patient, checked inside a transaction that locks the patient row; written once, never edited (withdraw and record a new one), withdrawing ends access at once, nothing deleted; only src/lib/family-consents.ts writes the table; audit actions family_consent_recorded and family_consent_withdrawn never hold names, relationships or scopes. VISIT NOTES: permission (visits.document to write, visits.review to review) AND relationship AND, for writing, being the visit's own assigned clinician. draft -> submitted -> reviewed; drafts editable by author, submitted locked; author never reviews own note; note only once the visit is in_progress or completed; one per visit; corrections after review are addenda. ADDENDA: after a note is REVIEWED the visit's own clinician adds a correction, additional information or late entry (max 2000 characters); needs visits.document, reach, being the visit's own clinician (even an administrator is refused), no addendum already waiting, fewer than 10, checked inside a transaction that locks the note row; written once (no edit, no delete); reviewing needs visits.review, reach, the addendum must belong to the visit in the request, never the author; submitted -> reviewed. TASKS: permission (tasks.read to see and finish, tasks.manage to create and cancel), REACH (a patient task is visible only to someone who can reach that patient), OWNERSHIP (administrative roles see all; others only tasks assigned to or created by them); non-administrative roles assign only to themselves; a patient task can only go to someone who reaches that patient; complete by assignee or admin role, cancel by creator or admin role; open -> done or cancelled, final; audit never holds titles. NOTIFICATIONS: table notifications (kind, resource id, no words); kinds task_assigned, task_completed, task_cancelled, note_reviewed, addendum_reviewed; never to yourself; own notices only, someone else's or made-up id gets the same not-found words and an audited denial; another organization and unknown kinds never written; best effort; page /notifications calls requireUser itself; bell with unread count in the app shell; no menu item. CARE PLANS: permission, relationship, and for WRITING being on the care team now; author never approves own plan; approving needs a goal and no other active plan; draft -> active -> completed, draft -> archived; one draft and one active plan per patient. DOCUMENTS: permission, relationship, CATEGORY (insurance and identification restricted to SUPER_ADMIN and ADMIN unless shared by an administrator); PDF/PNG/JPEG only, 2 MB, no duplicate bytes per patient; archive needs documents.delete; downloads audited; bytes in the database for the demo only, real storage is a paid decision, ask me first. REFERRALS: permission, reach, office-detail fields administrative only; received -> in_review -> accepted; decline and withdraw need a reason; one open referral per person. CARE TEAMS: permission, reach, active patient only, person must hold a role that may fill the place, same organization, one primary nurse per patient; ending sets an end date and deletes nothing. Made-up, unreachable and other-organization things all get the same words. Denials audited, audit never holds names or text.

SANDBOX RECIPE (cannot reach binaries.prisma.sh or unsplash.com; bash_tool is /bin/sh so use ". file", no rsync, never pkill -f with a pattern in your own command line (it kills your own shell), stop a server with fuser -k PORT/tcp if fuser exists; do NOT put "set -e" in a sourced file; every bash call has a 300 second limit; verify-access took well under a minute last time but still run slow things with timeout or in the background (setsid nohup script &, then poll; it survived between calls, and pg_ctlcluster 16 main start is harmless to repeat): apt-get update then apt-get install -y postgresql (plain install failed with 404 until apt-get update), run pg_ctlcluster 16 main start inside EVERY command, set the postgres password and create a database, DATABASE_URL must contain @localhost; work in a COPY: cp -r, npm ci --ignore-scripts, add engineType = "client" to the generator, npm i --no-save --ignore-scripts @prisma/adapter-pg@6.19.3 pg@8, pass the PrismaPg adapter in src/lib/prisma.ts and prisma/seed.ts, set PRISMA_SCHEMA_ENGINE_BINARY and PRISMA_QUERY_ENGINE_LIBRARY to a dummy executable plus PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1, prisma generate, apply migration SQL folders with psql from the GitHub clone, hand-write SQL only for models GitHub lacks (after I push, GitHub will have add_family_consents), npx next typegen before tsc, run npx next build directly with NODE_ENV=production (NODE_ENV=development makes it fail on /_global-error), scripts run with tsx --tsconfig tsconfig.scripts.json. Keep the real tree as the source of truth and copy changed files into the sandbox copy one by one. Render pages by inserting a sessions row (id, user_id, expires_at, created_at) and curl with cookie ccp_session=<id> against next start started in the same command as the curl calls (pages stream: the real content is after the Loading fallback, strip script tags before reading text). role_permissions has no id column (only role_id, permission_id). To test a rule holds, break it in the sandbox copy one rule at a time and confirm the verify script fails (a small python loop that patches one file, runs the script, restores the file works well); delete any stray rows a deliberate break leaves behind before the next run (and undo any permission rows you inserted). A race test that only fires two calls at once can pass by luck with the lock removed: prove a row lock deterministically by holding the row in an open transaction and checking the call waits.

MY COMMANDS after each delivery (Windows PowerShell): cd C:\Users\LENOVO\OneDrive\Desktop\compassionate-care-plus ; Remove-Item any files or folders the delivery names ; npm install ; npx prisma migrate dev --name <name> (only when a schema change ships; answer y to the prompt; THIS round: add_family_consents) ; npx prisma db seed ; npm run verify:access ; npm run verify:shell ; npm run verify:notes ; npm run verify:tasks ; npm run verify:notifications ; npm run verify:caregiver ; npm run verify:portal ; npm run verify:family ; npm run dev ; then git add . ; git commit ; git push. I run these ONE AT A TIME (a pasted block gets stuck on the migration y/N prompt).
