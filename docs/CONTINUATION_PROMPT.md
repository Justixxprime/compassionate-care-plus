CONTINUATION PROMPT (Cheliv / Compassionate Care Plus platform)

I'm attaching the latest project zip (ccp-e1-app-shell.zip). Read EVERY code file and every .md file top to end (start with docs/PROJECT_HANDOFF.md, NEXT_STEP.md, PHASE_STATUS.md, APP_SHELL.md, DOCUMENTS.md, REVIEW_MILESTONE_D.md). Clone https://github.com/Justixxprime/compassionate-care-plus.git and compare it to the zip. Find the exact stopping point before building. Explain non-coding things in ultra-simple language. End EVERY reply with a fresh, updated copy of this prompt, because I move between Claude accounts and come back to earlier ones. PACKAGE AND PRESENT THE ZIP BEFORE spending tool calls on extra verification.

STANDING RULES: no em dashes anywhere (code, docs, replies); no "placeholder / to be confirmed" wording on the public site; the four footer legal links stay unlinked and unflagged; cinematic, premium visual bar for public pages; every delivery includes a full project zip (exclude .env, node_modules, .next) plus present the files; full file contents, never diffs; after zipping, extract it fresh and diff against the working files; name-check src/app/layout.tsx (must have html, body, globals.css import), src/app/(app)/layout.tsx (must NOT have html or body) and src/app/(public)/page.tsx every time and say so; terminal and git commands at the END of every delivery. Next.js 16.3.5: read node_modules/next/dist/docs before writing code (an error screen gets retry(), not reset()). Prisma pinned 6.19.3, no new dependencies. The phone number is settled, do not raise it. Never ship migration SQL: I run `npx prisma migrate dev --name <name>` myself. Never put my .env in a zip. Zips never delete files, so name any file I must Remove-Item myself.

STATE (21 Sep 2026): Milestones A, B, C, D done. E0 confirmed on my machine (seed ran, verify:access 553 passed, coordinator and supervisor sign in). E1 is DELIVERED in ccp-e1-app-shell.zip and verified in the build environment (tsc, eslint, next build clean; verify:access 661 passed 0 failed; verify:shell 62 passed 0 failed; 15 sharing rules and 9 shell rules broken one at a time and each caught; pages fetched over HTTP as each demo account; signed-out visitors redirected; share and take back tested through the real server actions). NOT yet run on my machine: the steps below in MY COMMANDS. E1 contains: (1) the internal app shell, a route group src/app/(app)/ with a shared layout (sidebar, phone menu, skip link), menu decided on the server from real permissions (src/lib/app/navigation.ts), every page calls requireUser() itself (the shell is not the lock), loading, error (retry) and not-found screens, shared components in src/components/app (page-header, section, empty-state, no-access, stat-tile, data-table that becomes cards on phones), the five proof pages plus the dashboard moved inside it and restyled; (2) a real dashboard per permission (src/lib/app/dashboard.ts built only from existing services): needs-attention list, tiles, today's visits, referrals waiting with waiting time, patients without a primary nurse, plans to approve, recent activity for audit.read; (3) SHARING RESTRICTED DOCUMENTS (my decision): insurance cards and ID scans are now seen by SUPER_ADMIN and ADMIN only by default (a clinical supervisor no longer sees them); an administrator can share them with one named person, for one patient (all restricted documents including later ones) or one document, for 7 days, 30 days or until taken back; new table document_access_grants (migration add_document_access_grants, which I must run), new permission documents.grant (34 permissions; SUPER_ADMIN and ADMIN hold it, and the person must also BE an administrator role), screen /documents/sharing reached from Documents, a share is permission to look and download only, never to file, archive or share onward, reach still applies, every take-back ends access at once, audit events document_access_granted and document_access_revoked never hold names; (4) public photos from Unsplash for now (I do not want to tell my uncle yet; real photos at the last stage before showing): all in one file src/lib/site-images.ts (hero, who we serve on the homepage, about, who we serve page), drawn by a SiteImage component, credits and swap instructions in docs/IMAGES.md. I could not see the photographs or preview them in a browser in the build environment, so I may need to say if a crop or pick is wrong. 

ASK ME ONLY THIS (once): did all the commands below work, and do the four photos look right (say which to swap)? If I say yes, continue without asking for screenshots.

NEXT: E2 the Care Command Center: referral inbox with waiting time, accept-and-assign flow, patient list and profile, the care team panel with add and end and the worklist using src/lib/care-team.ts plus server actions, staff list, scheduling board, audit log page with filters (audit_logs has no organization column and getRecentAuditLog is not organization scoped: scope it). Then E3 clinical portal (needs a visit notes table), E4 caregiver portal phone-first, E5 patient and family portals later. Also open: /design-system is publicly reachable.

RULES (do not weaken). VISITS: permission (visits.read/create/update) AND relationship to the patient. Administrative roles (SUPER_ADMIN, ADMIN, CLINICAL_SUPERVISOR, CARE_COORDINATOR) see all and can schedule for any active care-team member of an active patient. Others see visits of actively assigned patients, schedule only for themselves, change only their own. scheduled -> in_progress -> completed; cancel and mark_missed only from scheduled; final states final. No double-booking. Times stored UTC, shown America/Chicago. CARE PLANS: permission (care_plans.read/create/update/approve), relationship (getPatientScope), and for WRITING also being on that patient's care team right now (isActiveCareTeamMember). Any team member can edit a draft. The author can never approve their own plan. Approving needs at least one goal and no other active plan. draft -> active -> completed, draft -> archived; active wording locked, only marking a goal met works. One draft and one active plan per patient. DOCUMENTS: permission (documents.read/upload/delete/grant), relationship, and CATEGORY: insurance and identification are restricted to SUPER_ADMIN and ADMIN by default and to anyone else only through an active, unexpired, unrevoked share made by an administrator (patient-wide or one document, to one person who holds documents.read and already reaches the patient); to everyone else a restricted document does not exist. Unknown categories fail closed. Filing restricted kinds needs an administrator role and organization reach; a share never allows filing, archiving or sharing onward. PDF/PNG/JPEG only, 2 MB, no duplicate bytes per patient, active patients only. Archive needs documents.delete (SUPER_ADMIN only). Downloads audited. Bytes live in the database for the demo only: real files need encrypted object storage and virus scanning, a paid decision, ask me first. REFERRALS: permission (referrals.read to see; referrals.manage to record, edit and decide), REACH (an unlinked referral is reachable by administrative scope only; a linked one by getPatientScope of its patient), FIELDS (office details = outside contact name and phone, office notes, decision note, administrative scope only, never selected for anyone else). received -> in_review -> accepted; decline and withdraw need a written reason; final states final; one open referral per person (name case-insensitive plus DOB). Accept links an existing active patient with the same name and DOB, or creates a new patient (needs patients.create, refused if that name and DOB exist, transaction with status guard); a new patient has no care team. CARE TEAMS: permission (care_team.read to look, care_team.manage to change), reach, then rules that belong to teams: active patient only, the person must really hold a role that may fill the place (primary_nurse and nurse need NURSE, caregiver needs CAREGIVER, checked in the database), same organization, nobody on a team twice, one primary nurse per patient. Ending sets an end date and deletes nothing; access ends at once; future visits are reported, never cancelled. Made-up, unreachable and other-organization things all get the same words. Denials audited, audit never holds names.

OPEN DECISIONS FOR ME (full list in docs/REVIEW_MILESTONE_D.md): the three Milestone E questions (defaults in use); nurses see colleagues' visits but cannot change them; any team nurse can edit a draft plan; where real document files will live (paid, ask first); whether ADMIN may archive documents and whether ADMIN should hold documents.grant (it does today; narrowing it to SUPER_ADMIN is a one-line seed change); the public /request-care form still says "Request received" and saves nothing; the repo is public and PROJECT_HANDOFF.md names the owner and says the site is a surprise (private repo or trim the file?).

SANDBOX RECIPE (build environment cannot reach binaries.prisma.sh or unsplash.com; bash_tool is /bin/sh so use ". file" not "source", there is no rsync, and never use pkill -f with a pattern that appears in your own command line): apt-get update then apt-get install -y postgresql, start with pg_ctlcluster 16 main start inside every command (it stops between calls), set the postgres password (su postgres -c "psql -c ...") and create a database, DATABASE_URL must contain @localhost; work in a COPY of the project: cp -r, npm ci --ignore-scripts, add engineType = "client" to the generator, npm i --no-save --ignore-scripts @prisma/adapter-pg@6.19.3 pg@8, pass the PrismaPg adapter in src/lib/prisma.ts and prisma/seed.ts, set PRISMA_SCHEMA_ENGINE_BINARY and PRISMA_QUERY_ENGINE_LIBRARY to a dummy executable plus PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1, prisma generate, apply migration SQL folders with psql (take the migrations from the GitHub clone since the zip lacks them; hand-write SQL only for models GitHub lacks, sandbox only: the document_access_grants table is the current example), npx next typegen before tsc, run npx next build directly, scripts run with tsx --tsconfig tsconfig.scripts.json. Keep the real project tree as the source of truth and re-sync it into the sandbox copy with a small prep script that re-applies the sandbox-only patches; never change the shipped source for the sandbox. Render pages by inserting a sessions row (id, user_id, expires_at, created_at) and curl with cookie ccp_session=<id> against next start (start it in the same command as the curl calls, because background servers die between calls). To test a server action over HTTP, take its id from .next/server/server-reference-manifest.json (match exportedName) and build the body with encodeReply from next/dist/compiled/react-server-dom-turbopack/client.edge in a Node script, then POST with a Next-Action header. To test that a rule really holds, break it in the sandbox copy one rule at a time and confirm verify:access or verify:shell fails each time.

MY COMMANDS after each delivery (Windows PowerShell): cd C:\Users\LENOVO\OneDrive\Desktop\compassionate-care-plus ; Remove-Item any files or folders the delivery names ; npm install ; npx prisma migrate dev --name <name> (only when a schema change ships) ; npx prisma db seed ; npm run verify:access ; npm run verify:shell ; npm run dev ; then git add . ; git commit ; git push.
