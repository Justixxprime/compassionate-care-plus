# CONTINUATION_PROMPT.md

Paste everything inside the box below as the FIRST message in another Claude account, and attach the latest project zip.
Claude refreshes this file (and the copy at the end of every reply) each round. Last refreshed: 20 September 2026.

```
CONTINUATION PROMPT (Cheliv / Compassionate Care Plus platform)

I'm attaching the latest project zip. Read EVERY code file and every .md file top to end (start with docs/PROJECT_HANDOFF.md, NEXT_STEP.md, PHASE_STATUS.md, REVIEW_MILESTONE_D.md, REFERRALS.md, DOCUMENTS.md, CARE_PLANS.md, VISITS.md). Clone https://github.com/Justixxprime/compassionate-care-plus.git and compare it to the zip. Find the exact stopping point before building. Explain non-coding things in ultra-simple language. End EVERY reply with a fresh, updated copy of this prompt, because I move between Claude accounts and come back to earlier ones.

STANDING RULES: no em dashes anywhere; no "placeholder / to be confirmed" wording on the public site; the four footer legal links stay unlinked and unflagged; cinematic, premium visual bar for public pages; every delivery includes a full project zip (exclude .env, node_modules, .next) plus present the files; full file contents, never diffs; after zipping, extract it fresh and diff against the working files; name-check src/app/layout.tsx (must have html, body, globals.css import) and src/app/(public)/page.tsx every time and say so; terminal and git commands at the END of every delivery. Next.js 16.3.5: read node_modules/next/dist/docs before writing code. Prisma pinned 6.19.3, no new dependencies. The phone number is settled, do not raise it. Never ship migration SQL: I run `npx prisma migrate dev --name <name>` myself. Never put my .env in a zip. Zips never delete files, so name any file I must Remove-Item myself.

STATE (20 Sep 2026): Milestones A, B, C done. Milestone D (patients, care team, visits, care plans, documents, referrals) is finished. Patients, care team, visits, care plans confirmed on my machine; documents committed by me (GitHub commit 5f3d492 has the add_documents migration). REFERRALS delivered in this zip, written and verified in the build environment: tsc, eslint, next build clean; verify:access 407 passed, 0 failed (was 254); thirteen rules broken one at a time in a copy and the script failed each time; real HTTP: /referrals page as admin (six referrals with office details), nurse (only Eleanor's, no office details, no other person's name), nurse2 (only Marcus's); the create, edit and status-change server actions and the document upload action called over real HTTP with React's own encodeReply body: admin succeeded, nurse and signed-out refused, nothing saved for refused calls. NOT yet run on my machine: migration add_referrals, seed, verify:access 407, the /referrals click-through, and clicking the Record a referral form in a real browser. Files: Referral model; src/lib/referrals.ts, referral-constants.ts, referrals-actions.ts; src/app/referrals/*; time.ts parseCalendarDate and formatCalendarDate; seed gives NURSE referrals.read and adds 6 synthetic referrals; verify-access.ts sections 2d and 12a to 12i; docs/REFERRALS.md and docs/REVIEW_MILESTONE_D.md.

FIRST, ask me only what the repo cannot show: did `npx prisma migrate dev --name add_referrals`, `npx prisma db seed` (expect "6 synthetic demo referrals ready"), `npm run verify:access` (expect 407 passed) and the /referrals click-through as all three demo accounts work, and did the "Record a referral" form work when I used it by hand (empty name, impossible date, duplicate person refused; a real one appears as Received; Accept on Walter Brennan creates a patient)? Also ask whether real photography has arrived. Fix anything I report before building more. If I say everything worked, do not ask me to screenshot each step: continue.

NEXT: read docs/REVIEW_MILESTONE_D.md with me. It ends with three questions that decide Milestone E: (1) which portal first (recommended: the administrator's Care Command Center), (2) whether the recommended permission sets for CARE_COORDINATOR (referrals.read and manage, patients.read, visits.read/create/update, care-team assignment) and CLINICAL_SUPERVISOR (care plan approval, read of visits and documents) are right, (3) whether the public /request-care form is wired up now as its own slice or later with the wording changed. Do NOT start Milestone E before I answer. When I do, begin with E0: extract the copied loadActor and auditDenied helpers into one shared file (guarded by verify:access), add care-team assignment as a real tested service, add the agreed permission sets. Then E1 the internal app shell, E2 Care Command Center, E3 clinical portal (needs a visit notes table), E4 caregiver portal phone-first, E5 patient and family portals later.

RULES (do not weaken). VISITS: permission (visits.read/create/update) AND relationship to the patient. Administrative roles (SUPER_ADMIN, ADMIN, CLINICAL_SUPERVISOR, CARE_COORDINATOR) see all and can schedule for any active care-team member of an active patient. Others see visits of actively assigned patients, schedule only for themselves, change only their own. scheduled -> in_progress -> completed; cancel and mark_missed only from scheduled; final states final. No double-booking. Times stored UTC, shown America/Chicago. CARE PLANS: THREE questions: permission (care_plans.read/create/update/approve), relationship (getPatientScope), and for WRITING also being on that patient's care team right now (isActiveCareTeamMember), so SUPER_ADMIN reads and approves but cannot write for a patient it is not on the team of. Any team member can edit a draft. The author can never approve their own plan (four eyes). Approving needs at least one goal and no other active plan for the patient. draft -> active -> completed, draft -> archived; final states final. Active wording and goals are locked, only marking a goal met works. One draft and one active plan per patient. Denials audited as access_denied with vague not-found messages for anything out of reach. DOCUMENTS: THREE questions: permission (documents.read/upload/delete), relationship (getPatientScope), and CATEGORY: insurance and identification are restricted to administrative roles, and to a nurse a restricted document does not exist (missing from lists, and the download route returns the same 404 words as a made-up id). Unknown categories fail closed. No care-team question for documents. File type read from the bytes, PDF/PNG/JPEG only, 2 MB, no duplicate bytes per patient, active patients only. Archive needs documents.delete (SUPER_ADMIN only), hides everywhere, keeps the row and bytes. Downloads audited (document_downloaded). Bytes live in the database for the demo only: real patient files need encrypted object storage and virus scanning, a paid decision, ask me first. REFERRALS: THREE questions: permission (referrals.read to see; referrals.manage to record, edit and decide), REACH (an unlinked referral, about someone not yet a patient, is reachable by administrative scope only; a linked one by getPatientScope of its patient), and FIELDS (summary and clinical reason for anyone with reach; office details = outside contact name and phone, office notes, decision note, administrative scope only, columns never selected for anyone else). No care-team question. Recording needs administrative scope. NURSE holds referrals.read only; ADMIN and SUPER_ADMIN hold both. received -> in_review (start_review) -> accepted (accept); decline and withdraw from received or in_review, both need a written reason; final states final; only open referrals editable; one open referral per person (name case-insensitive plus DOB). Accept links an existing active patient with the exact same name and DOB, or creates a new patient (needs patients.create, refused if that name and DOB already exist, transaction with status guard); a new patient has no care team. Vague not-found, denials audited, audit events referral_created, referral_updated, referral_review_started, referral_accepted, referral_declined, referral_withdrawn, patient_created.

OPEN DECISIONS FOR ME (full list with recommendations in docs/REVIEW_MILESTONE_D.md): the three Milestone E questions above; nurses see colleagues' visits on shared patients but cannot change them; any nurse on the team can edit a draft plan, not only its author; only ADMIN and SUPER_ADMIN can approve plans; where real document files will live (encrypted object storage, priced by you with me first); whether ADMIN may archive documents and whether consents should be restricted; the repo is public and PROJECT_HANDOFF.md names the owner and says the site is a surprise (private repo or trim the file?; README.md also promises private "at the database phase at the latest").

SANDBOX RECIPE for real testing (build environment cannot reach binaries.prisma.sh): apt-get update then apt-get install -y postgresql, start with pg_ctlcluster 16 main start inside every command (it stops between calls, so does any background server), set the postgres password and create a database, DATABASE_URL must contain @localhost; work in a COPY of the project: npm ci --ignore-scripts, add engineType = "client" to the generator, npm i --no-save --ignore-scripts @prisma/adapter-pg@6.19.3 pg@8, pass the PrismaPg adapter in src/lib/prisma.ts and prisma/seed.ts, set PRISMA_SCHEMA_ENGINE_BINARY and PRISMA_QUERY_ENGINE_LIBRARY to a dummy executable plus PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1, prisma generate, apply migration SQL folders with psql (take the migrations from the GitHub clone since the zip lacks them; hand-write SQL for new models, sandbox only), npx next typegen before tsc, next build works (run npx next build directly, not npm run build), scripts run with tsx --tsconfig tsconfig.scripts.json, render pages by inserting a sessions row and curl with cookie ccp_session=<id> against next start (start it in the same command as the curl calls; do not use pgrep to test whether it is running). Never change the shipped source for this. To test a server action over HTTP, take its id from .next/server/server-reference-manifest.json and build the body with encodeReply from next/dist/compiled/react-server-dom-turbopack/client.edge in a Node script, then POST with a Next-Action header (a hand-built curl multipart body does not work).

MY COMMANDS after each delivery (Windows PowerShell): cd C:\Users\LENOVO\OneDrive\Desktop\compassionate-care-plus ; npm install ; npx prisma migrate dev --name <name> ; npx prisma db seed ; npm run verify:access ; npm run dev ; then git add . ; git commit ; git push.
```
