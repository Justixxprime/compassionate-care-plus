CONTINUATION PROMPT (Cheliv / Compassionate Care Plus platform)

I'm attaching the latest project zip (ccp-e3r2-e4r2.zip). Read EVERY code file and every .md file top to end (start with docs/PROJECT_HANDOFF.md, NEXT_STEP.md, PHASE_STATUS.md, APP_SHELL.md, DOCUMENTS.md, VISIT_NOTES.md, TASKS.md, NOTIFICATIONS.md, REVIEW_MILESTONE_D.md). Clone https://github.com/Justixxprime/compassionate-care-plus.git and compare it to the zip. Find the exact stopping point before building. Explain non-coding things in ultra-simple language. End EVERY reply with a fresh, updated copy of this prompt, because I move between Claude accounts and come back to earlier ones. PACKAGE AND PRESENT THE ZIP BEFORE spending tool calls on extra verification. Budget your tool calls so the zip is presented before you run out.

STANDING RULES: no em dashes anywhere (code, docs, replies); no "placeholder / to be confirmed" wording on the public site; the four footer legal links stay unlinked and unflagged; cinematic, premium visual bar for public pages; every delivery includes a full project zip (exclude .env, node_modules, .next) plus present the files; full file contents, never diffs; after zipping, extract it fresh and diff against the working files; name-check src/app/layout.tsx (must have html, body, globals.css import), src/app/(app)/layout.tsx (must NOT have html or body) and src/app/(public)/page.tsx every time and say so; terminal and git commands at the END of every delivery. Next.js 16.3.5: read node_modules/next/dist/docs before writing code (an error screen gets retry(), not reset()). Prisma pinned 6.19.3, no new dependencies. The phone number is settled, do not raise it. Never ship migration SQL: I run npx prisma migrate dev --name <name> myself. Never put my .env in a zip. Zips never delete files, so name any file I must Remove-Item myself. My delivered zips carry only migration_lock.toml and the earliest 3 migration folders (expected, not a bug): every schema change after that was applied by me with npx prisma migrate dev, so when verifying in a sandbox pull the real migration history from the GitHub clone. In the sandbox, hand-write SQL only for models GitHub lacks (sandbox only, never shipped).

STATE (23 Sep 2026): Milestones A, B, C, D done. E0 to E4 round 1 (visit notes, tasks) confirmed on my machine. E3 round 2 (note addenda) and E4 round 2 (notifications) are DELIVERED in ccp-e3r2-e4r2.zip and tested in the build environment with real Postgres: verify:access 661, verify:shell 62, verify:tasks 40, verify:notes 93, verify:notifications 44, tsc clean, ten addendum rules and seven notification rules broken on purpose and each caught. NOT yet run on my machine: the commands below and the click-through. NEEDS A REAL MIGRATION: npx prisma migrate dev --name add_addenda_and_notifications (tables visit_note_addenda and notifications). The zip also restores the correct src/app/(app)/documents/page.tsx (GitHub commit 25b28d4 had the Sharing page there by mistake); I must commit and push it.
ADDENDA: after a note is REVIEWED, the visit's own clinician adds a correction, additional information or late entry (max 2000 characters); needs visits.document, reach, being the visit's own clinician (even an administrator is refused), no addendum already waiting, fewer than 10, checked inside a transaction that locks the note row; written once (no edit, no delete); reviewing needs visits.review, reach, the addendum must belong to the visit in the request, never the author; submitted -> reviewed; addenda appear in the /visits review list; audit visit_note_addendum_added and visit_note_addendum_reviewed. NOTIFICATIONS: table notifications (kind, resource id, no words); kinds task_assigned, task_completed, task_cancelled, note_reviewed, addendum_reviewed; never to yourself; own notices only, someone else's or made-up id gets the same not-found words and an audited denial; another organization and unknown kinds never written; best effort; page /notifications calls requireUser itself; bell with unread count in the app shell; no menu item.

ASK ME ONLY THIS (once, after my normal commands succeed): (1) as demo nurse write, save and submit a note, then as demo supervisor click Mark reviewed; the nurse's bell shows 1, open it, Mark read; (2) as the nurse add a correction addendum, then as the supervisor mark it reviewed; (3) as demo admin give the nurse a task and check the nurse's bell shows a notice that opens Tasks. If all work, treat E3 round 2 and E4 round 2 as confirmed.

NEXT (once confirmed): my choice of the caregiver portal (phone first: today's visits, check in and out, task checklist; the CAREGIVER role holds no permissions today, so this needs a permission design and verify:access updates), or the open items: audit log pagination past 200 rows, a real "create staff account" flow before Milestone F, /design-system publicly reachable, /request-care saves nothing, repo is public and PROJECT_HANDOFF.md names the owner and says the site is a surprise, whether supervisors should be kept away from restricted documents. Full list in docs/REVIEW_MILESTONE_D.md.

RULES (do not weaken). VISITS: permission (visits.read/create/update) AND relationship. Administrative roles (SUPER_ADMIN, ADMIN, CLINICAL_SUPERVISOR, CARE_COORDINATOR) see all and schedule for any active care-team member of an active patient. Others see visits of actively assigned patients, schedule only for themselves, change only their own. scheduled -> in_progress -> completed; cancel and mark_missed only from scheduled; final states final. No double-booking. Times stored UTC, shown America/Chicago. VISIT NOTES: permission (visits.document to write, visits.review to review) AND relationship AND, for writing, being the visit's own assigned clinician. draft -> submitted -> reviewed; drafts editable by author, submitted locked; author never reviews own note; note only once the visit is in_progress or completed; one per visit; corrections after review are addenda. TASKS: permission (tasks.read to see and finish, tasks.manage to create and cancel), REACH (a patient task is visible only to someone who can reach that patient), OWNERSHIP (administrative roles see all; others only tasks assigned to or created by them); non-administrative roles assign only to themselves; a patient task can only go to someone who reaches that patient; complete by assignee or admin role, cancel by creator or admin role; open -> done or cancelled, final; audit never holds titles. CARE PLANS: permission, relationship, and for WRITING being on the care team now; author never approves own plan; approving needs a goal and no other active plan; draft -> active -> completed, draft -> archived; one draft and one active plan per patient. DOCUMENTS: permission, relationship, CATEGORY (insurance and identification restricted to SUPER_ADMIN and ADMIN unless shared by an administrator); PDF/PNG/JPEG only, 2 MB, no duplicate bytes per patient; archive needs documents.delete; downloads audited; bytes in the database for the demo only, real storage is a paid decision, ask me first. REFERRALS: permission, reach, office-detail fields administrative only; received -> in_review -> accepted; decline and withdraw need a reason; one open referral per person. CARE TEAMS: permission, reach, active patient only, person must hold a role that may fill the place, same organization, one primary nurse per patient; ending sets an end date and deletes nothing. Made-up, unreachable and other-organization things all get the same words. Denials audited, audit never holds names or text.

SANDBOX RECIPE (cannot reach binaries.prisma.sh or unsplash.com; bash_tool is /bin/sh so use ". file", no rsync, never pkill -f with a pattern in your own command line; do NOT put "set -e" in a sourced file): apt-get update then apt-get install -y postgresql (plain install failed with 404 until apt-get update), run pg_ctlcluster 16 main start inside EVERY command (it stops between calls), set the postgres password and create a database, DATABASE_URL must contain @localhost; work in a COPY: cp -r, npm ci --ignore-scripts, add engineType = "client" to the generator, npm i --no-save --ignore-scripts @prisma/adapter-pg@6.19.3 pg@8, pass the PrismaPg adapter in src/lib/prisma.ts and prisma/seed.ts, set PRISMA_SCHEMA_ENGINE_BINARY and PRISMA_QUERY_ENGINE_LIBRARY to a dummy executable plus PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1, prisma generate, apply migration SQL folders with psql from the GitHub clone, npx next typegen before tsc, run npx next build directly, scripts run with tsx --tsconfig tsconfig.scripts.json. Keep the real tree as the source of truth and re-sync with a small prep script (tar copy, then re-apply the sandbox-only patches). Render pages by inserting a sessions row (id, user_id, expires_at, created_at) and curl with cookie ccp_session=<id> against next start started in the same command as the curl calls. To test a rule holds, break it in the sandbox copy one rule at a time and confirm the verify script fails; delete any stray rows a deliberate break leaves behind before the next run.

MY COMMANDS after each delivery (Windows PowerShell): cd C:\Users\LENOVO\OneDrive\Desktop\compassionate-care-plus ; Remove-Item any files or folders the delivery names ; npm install ; npx prisma migrate dev --name <name> (only when a schema change ships) ; npx prisma db seed ; npm run verify:access ; npm run verify:shell ; npm run verify:notes ; npm run verify:tasks ; npm run verify:notifications ; npm run dev ; then git add . ; git commit ; git push.
