# CONTINUATION_PROMPT.md

Paste everything inside the box below as the FIRST message in another Claude account, and attach the latest project zip.
Claude refreshes this file (and the copy at the end of every reply) each round. Last refreshed: 21 September 2026.

```
CONTINUATION PROMPT (Cheliv / Compassionate Care Plus platform)

I'm attaching the latest project zip (ccp-e0-care-teams.zip). Read EVERY code file and every .md file top to end (start with docs/PROJECT_HANDOFF.md, NEXT_STEP.md, PHASE_STATUS.md, CARE_TEAMS.md, REVIEW_MILESTONE_D.md). Clone https://github.com/Justixxprime/compassionate-care-plus.git and compare it to the zip. Find the exact stopping point before building. Explain non-coding things in ultra-simple language. End EVERY reply with a fresh, updated copy of this prompt, because I move between Claude accounts and come back to earlier ones. PACKAGE AND PRESENT THE ZIP BEFORE spending tool calls on extra verification.

STANDING RULES: no em dashes anywhere; no "placeholder / to be confirmed" wording on the public site; the four footer legal links stay unlinked and unflagged; cinematic, premium visual bar for public pages; every delivery includes a full project zip (exclude .env, node_modules, .next) plus present the files; full file contents, never diffs; after zipping, extract it fresh and diff against the working files; name-check src/app/layout.tsx (must have html, body, globals.css import) and src/app/(public)/page.tsx every time and say so; terminal and git commands at the END of every delivery. Next.js 16.3.5: read node_modules/next/dist/docs before writing code. Prisma pinned 6.19.3, no new dependencies. The phone number is settled, do not raise it. Never ship migration SQL: I run `npx prisma migrate dev --name <name>` myself. Never put my .env in a zip. Zips never delete files, so name any file I must Remove-Item myself.

STATE (21 Sep 2026): Milestones A, B, C, D done. Referrals CONFIRMED on my machine (I recorded and accepted a referral, which created a patient; nurse two saw only Marcus's referral). Milestone E step E0 (no screens) is delivered in the zip, verified in the build environment (tsc, eslint, next build clean; verify:access 553 passed, 0 failed; 18 rules broken one at a time and the script failed on each): shared helpers in src/lib/auth/actor.ts (loadActor, auditDenied, auditAllowed, guarded by verify:access section 0), care team service src/lib/care-team.ts (add, end, worklist of patients needing a primary nurse; one primary nurse enforced in a retried serializable transaction; vague not-found words; audit events care_team_assigned and care_team_ended), permissions care_team.read and care_team.manage (33 total), real permission sets for CARE_COORDINATOR and CLINICAL_SUPERVISOR, demo accounts demo.coordinator@cheliv.test and demo.supervisor@cheliv.test. No migration in E0. NOT yet run on my machine: `npx prisma db seed` (expect "Demo coordinator ready" and "Demo supervisor ready") and `npm run verify:access` (expect 553 passed). I answered none of the three Milestone E review questions, so E0 used the recommended defaults: Care Command Center first, the recommended permission sets (plus patients.create for the coordinator, needed to accept a referral about someone new; nurses hold no care team permission), public request-care form later.

FIRST, ask me only what the repo cannot show: did `npx prisma db seed` and `npm run verify:access` (553 passed) work? Also ask (once) whether real photography has arrived (public/images is empty) and whether supervisors should be kept away from restricted documents (insurance card, ID scan; today a supervisor with documents.read sees them because "restricted" means administrative roles). Fix anything I report before building more. If I say everything worked, do not ask me to screenshot each step: continue.

NEXT: E1, the internal app shell. One shared layout for signed-in staff, navigation that shows only what the account may use (decided on the server from real permissions, never from the browser), a real dashboard per role, consistent tables and empty states, phone-friendly, and the five plain proof pages (/patients, /visits, /care-plans, /documents, /referrals) moved inside it. Every page and action still re-checks on the server. Then E2 the Care Command Center (referral inbox with waiting time, accept-and-assign flow, patient list and profile, the care team panel with add and end and the worklist using src/lib/care-team.ts plus server actions, staff list, scheduling board, audit log page with filters), E3 clinical portal (needs a visit notes table), E4 caregiver portal phone-first, E5 patient and family portals later.

RULES (do not weaken). VISITS: permission (visits.read/create/update) AND relationship to the patient. Administrative roles (SUPER_ADMIN, ADMIN, CLINICAL_SUPERVISOR, CARE_COORDINATOR) see all and can schedule for any active care-team member of an active patient. Others see visits of actively assigned patients, schedule only for themselves, change only their own. scheduled -> in_progress -> completed; cancel and mark_missed only from scheduled; final states final. No double-booking. Times stored UTC, shown America/Chicago. CARE PLANS: permission (care_plans.read/create/update/approve), relationship (getPatientScope), and for WRITING also being on that patient's care team right now (isActiveCareTeamMember). Any team member can edit a draft. The author can never approve their own plan. Approving needs at least one goal and no other active plan. draft -> active -> completed, draft -> archived; active wording locked, only marking a goal met works. One draft and one active plan per patient. DOCUMENTS: permission (documents.read/upload/delete), relationship, and CATEGORY: insurance and identification restricted to administrative roles, and to a nurse a restricted document does not exist. Unknown categories fail closed. PDF/PNG/JPEG only, 2 MB, no duplicate bytes per patient, active patients only. Archive needs documents.delete (SUPER_ADMIN only). Downloads audited. Bytes live in the database for the demo only: real files need encrypted object storage and virus scanning, a paid decision, ask me first. REFERRALS: permission (referrals.read to see; referrals.manage to record, edit and decide), REACH (an unlinked referral is reachable by administrative scope only; a linked one by getPatientScope of its patient), FIELDS (office details = outside contact name and phone, office notes, decision note, administrative scope only, never selected for anyone else). received -> in_review -> accepted; decline and withdraw need a written reason; final states final; one open referral per person (name case-insensitive plus DOB). Accept links an existing active patient with the same name and DOB, or creates a new patient (needs patients.create, refused if that name and DOB exist, transaction with status guard); a new patient has no care team. CARE TEAMS: permission (care_team.read to look, care_team.manage to change), reach, then rules that belong to teams: active patient only, the person must really hold a role that may fill the place (primary_nurse and nurse need NURSE, caregiver needs CAREGIVER, checked in the database), same organization, nobody on a team twice, one primary nurse per patient. Ending sets an end date and deletes nothing; access ends at once; future visits are reported, never cancelled. Made-up, unreachable and other-organization things all get the same words. Denials audited, audit never holds names.

OPEN DECISIONS FOR ME (full list in docs/REVIEW_MILESTONE_D.md): the three Milestone E questions (defaults in use); supervisor access to restricted documents; nurses see colleagues' visits but cannot change them; any team nurse can edit a draft plan; where real document files will live (paid, ask first); whether ADMIN may archive documents; the public /request-care form still says "Request received" and saves nothing; the repo is public and PROJECT_HANDOFF.md names the owner and says the site is a surprise (private repo or trim the file?).

SANDBOX RECIPE (build environment cannot reach binaries.prisma.sh; bash_tool is /bin/sh so use ". file" not "source", and there is no rsync): apt-get update then apt-get install -y postgresql, start with pg_ctlcluster 16 main start inside every command (it stops between calls), set the postgres password (su postgres -c "psql -c ...") and create a database, DATABASE_URL must contain @localhost; work in a COPY of the project: cp -r, npm ci --ignore-scripts, add engineType = "client" to the generator, npm i --no-save --ignore-scripts @prisma/adapter-pg@6.19.3 pg@8, pass the PrismaPg adapter in src/lib/prisma.ts and prisma/seed.ts, set PRISMA_SCHEMA_ENGINE_BINARY and PRISMA_QUERY_ENGINE_LIBRARY to a dummy executable plus PRISMA_ENGINES_CHECKSUM_IGNORE_MISSING=1, prisma generate, apply migration SQL folders with psql (take the migrations from the GitHub clone since the zip may lack them; hand-write SQL only for models GitHub lacks, sandbox only), npx next typegen before tsc, run npx next build directly, scripts run with tsx --tsconfig tsconfig.scripts.json. Keep the real project tree as the source of truth and re-sync it into the sandbox copy with a small prep script that re-applies the sandbox-only patches; never change the shipped source for the sandbox. Render pages by inserting a sessions row and curl with cookie ccp_session=<id> against next start (start it in the same command as the curl calls). To test a server action over HTTP, take its id from .next/server/server-reference-manifest.json and build the body with encodeReply from next/dist/compiled/react-server-dom-turbopack/client.edge in a Node script, then POST with a Next-Action header. To test that a rule really holds, break it in the sandbox copy one rule at a time and confirm verify:access fails each time (permission-set rules can be broken directly in the sandbox database).

MY COMMANDS after each delivery (Windows PowerShell): cd C:\Users\LENOVO\OneDrive\Desktop\compassionate-care-plus ; npm install ; npx prisma migrate dev --name <name> (only when a schema change ships) ; npx prisma db seed ; npm run verify:access ; npm run dev ; then git add . ; git commit ; git push.
```
