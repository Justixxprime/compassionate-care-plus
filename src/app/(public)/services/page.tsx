import { ComingSoonPage } from "@/components/marketing/coming-soon-page";

export default function ServicesPage() {
  return (
    <ComingSoonPage
      title="Services"
      phase="Phase 5 — public service pages"
      body="Individual pages for each confirmed service will live here, with an overview, what to expect, and how to request that specific service."
    />
  );
}
