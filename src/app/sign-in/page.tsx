export default function SignInPage() {
  return (
    <div className="mx-auto flex min-h-screen max-w-sm flex-col justify-center px-6">
      <p className="text-label font-medium text-slate">
        Milestone C — authentication
      </p>
      <h1 className="mt-2 font-display text-h1 text-ink">Sign in</h1>
      <p className="mt-4 text-body text-slate">
        Real sign in - for patients, families, caregivers, clinicians and
        administrators - is built once the database and authentication
        spine exist. Not yet available.
      </p>
    </div>
  );
}
